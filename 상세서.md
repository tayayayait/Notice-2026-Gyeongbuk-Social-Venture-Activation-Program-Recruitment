# UI/UX 구현 상세서 (경북 농업 부산물 업사이클링 원료 공급 SaaS)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 문서 버전 | v1.0 | 전체 | 본 문서는 디자인/개발이 동일 기준으로 구현하는 UI 규격서다. |
| 작성일 | 2026-02-26 | 전체 | 변경 시 버전/작성일을 갱신한다. |
| 대상 디바이스 | Web(반응형), Mobile(모바일 웹/앱 WebView) | 전체 | Admin/Buyer는 Web 우선, Farmer/Logistics는 Mobile 우선. |
| 기본 테마 | Light only | 전체 | 다크 모드는 `가정(Assumptions)`에 따라 범위 밖이다. |
| 디자인 키워드 | Trust(Blue) + Eco(Green) | 전체 | Primary=Green, Info/Focus=Blue. 색만으로 상태를 구분하지 않는다. |

---

## 1) 디자인 토큰 (Design Tokens)

### 1.1 색상 토큰 (Color)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| color.brand.green.50 | #F2FBF5 | 배경 틴트 | 넓은 면적 배경에만 사용(텍스트 금지). |
| color.brand.green.100 | #DCF3E4 | 배경/배지 | 배지 배경, 선택 영역의 약한 강조. |
| color.brand.green.600 | #1E7B3A | Primary | 주요 CTA(수거 요청/샘플 신청/저장). 텍스트는 `#FFFFFF`. |
| color.brand.green.700 | #186632 | Primary hover/active | hover=700, active=800 수준으로 더 진하게. |
| color.brand.green.800 | #145A2C | Primary active | active에서만 사용(면적 사용 금지). |
| color.brand.blue.50 | #EFF6FF | 정보 틴트 | 정보 배너/가이드 영역 배경. |
| color.brand.blue.600 | #2563EB | Info/Focus | 링크/포커스 링/정보 강조. Primary 버튼 배경으로는 사용하지 않는다. |
| color.brand.blue.700 | #1D4ED8 | Info hover/active | 링크 hover/active, 정보 버튼 active. |
| color.neutral.0 | #FFFFFF | Surface | 카드/모달/입력 배경 기본. |
| color.neutral.50 | #F7FAF8 | Canvas | 앱 전체 배경(대시보드 캔버스). |
| color.neutral.100 | #F1F5F9 | Sub-surface | 테이블 헤더/인풋 hover 배경 등. |
| color.neutral.200 | #E2E8F0 | Border light | 구분선/테이블 라인. |
| color.neutral.300 | #CBD5E1 | Border | 입력 기본 보더, 비활성 경계. |
| color.neutral.600 | #475569 | Text muted | 보조 텍스트(설명/메타). `#64748B` 이하 사용 금지(가독성). |
| color.neutral.900 | #0F172A | Text strong | 본문/제목 기본. |
| color.semantic.success.600 | #16A34A | 성공 | 완료/승인/정상 상태. `brand.green`과 혼용 시 의미를 우선한다. |
| color.semantic.warning.600 | #D97706 | 경고 | 주의/확인 필요. 텍스트는 `#0F172A` 우선. |
| color.semantic.danger.600 | #DC2626 | 위험/삭제 | 오류/삭제/거절. Primary를 대체하지 않는다. |
| color.semantic.info.600 | #2563EB | 정보 | 안내/참고. `brand.blue.600`과 동일 값으로 둔다. |
| color.state.focus.ring | #2563EB | 포커스 링 | 2px 내부 링 + 4px 외곽 글로우를 사용한다(아래 컴포넌트 규격 참고). |
| color.state.overlay.scrim | #0F172A66 | 모달/드로어 오버레이 | 오버레이는 최소 40% 불투명도(66)로 본문 대비 확보. |
| color.state.disabled.fg | #94A3B8 | disabled 텍스트 | disabled는 대비를 유지하되(배경과 구분) 정보 전달 텍스트는 금지. |
| color.state.disabled.bg | #F1F5F9 | disabled 배경 | disabled는 `cursor: not-allowed` 적용. |
| color.state.error.bg | #FEF2F2 | 오류 배경 | 인풋/배너 error 틴트. |
| color.state.error.border | #FCA5A5 | 오류 보더 | 인풋 error 보더. |
| color.state.error.fg | #B91C1C | 오류 텍스트 | 에러 메시지/아이콘. |

### 1.2 데이터 시각화 토큰 (Chart/Map)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| color.chart.series.1 | #1E7B3A | 시리즈 1 | 동일 의미는 동일 색을 유지한다(필터 변경에도 색 고정). |
| color.chart.series.2 | #2563EB | 시리즈 2 | 시리즈는 최대 6개 권장, 8개 초과 시 패턴/마커를 추가한다. |
| color.chart.series.3 | #0F766E | 시리즈 3 | 색만으로 구분하지 말고 범례/라벨을 항상 제공한다. |
| color.chart.series.4 | #D97706 | 시리즈 4 | 경고색(amber)은 시리즈에 쓸 수 있으나 오류 의미로 쓰지 않는다. |
| color.chart.series.5 | #DC2626 | 시리즈 5 | 빨강은 "감소/위험" 의미가 섞이지 않도록 범례 문구를 명확히. |
| color.chart.series.6 | #475569 | 시리즈 6 | 비교 기준/기준선에 주로 사용. |
| color.chart.gridline | #E2E8F0 | 그리드 라인 | 1px, 과도한 강조 금지. |
| color.chart.axis | #94A3B8 | 축/틱 | 축 라벨은 최소 12px 이상. |
| color.chart.tooltip.bg | #0F172AE6 | 툴팁 배경 | 90% 불투명( E6 ), 텍스트 `#FFFFFF`. |
| color.map.pin.available | #1E7B3A | 수거 가능 핀 | 지도 핀은 최소 24px, 선택 시 32px로 확대. |
| color.map.pin.scheduled | #2563EB | 예정 핀 | 예정/예약 상태. |
| color.map.pin.limited | #D97706 | 제한 핀 | 물량 부족/시간 제한. |
| color.map.pin.blocked | #DC2626 | 불가 핀 | 법적/행정 불가. |

### 1.3 타이포그래피 토큰 (Typography)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| font.family.base | Pretendard, "Noto Sans KR", system-ui, -apple-system, "Segoe UI", sans-serif | 전체 | 한글 가독성 우선. 숫자/단위가 많으므로 자간 과도한 폰트 금지. |
| font.family.mono | ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace | 코드/ID | API 키/식별자/수치 표기. |
| font.weight.regular | 400 | 본문 | 본문 기본. |
| font.weight.medium | 500 | 라벨/버튼 | 버튼/탭/필터 칩. |
| font.weight.semibold | 600 | 섹션 타이틀 | 카드 타이틀/테이블 헤더. |
| font.size.display | 32px | 대시보드 핵심 수치 | 모바일에서는 28px로 다운. |
| font.size.h1 | 24px | 페이지 타이틀 | Web 기본. |
| font.size.h2 | 20px | 섹션 타이틀 | 카드 그룹/모듈 타이틀. |
| font.size.h3 | 18px | 카드 타이틀 | KPI 카드, 패널 제목. |
| font.size.body | 16px | 본문 | 모바일 최소 16px(축소 금지). |
| font.size.body2 | 14px | 보조 본문 | 테이블/필터/헬퍼 텍스트. |
| font.size.caption | 12px | 캡션/메타 | 타임스탬프/단위. |
| line.height.tight | 1.25 | 제목 | 헤딩용. |
| line.height.base | 1.5 | 본문 | 기본 본문. |
| line.height.relaxed | 1.75 | 긴 설명 | 도움말/가이드. |
| text.maxLineLength | 72ch | 긴 문단 | 설명 문단은 72ch 초과 금지(가독성). |

### 1.4 간격 토큰 (Spacing, 4px 기반)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| space.0 | 0px | 리셋 | 예외적으로만 사용. |
| space.1 | 4px | 아이콘-텍스트 간격 | 가장 작은 간격. |
| space.2 | 8px | 폼 요소 간격 | 라벨-필드, 칩 내부 여백 등. |
| space.3 | 12px | 카드 내부 보조 간격 | 본문/보조텍스트 분리. |
| space.4 | 16px | 기본 패딩 | 카드/패널 기본 padding. |
| space.5 | 20px | 섹션 간격 | 필터 바-컨텐츠. |
| space.6 | 24px | 섹션 패딩 | 페이지 섹션 여백. |
| space.8 | 32px | 큰 구획 분리 | 대시보드 행 간격. |
| space.10 | 40px | 페이지 상하 여백 | Web에서 섹션 시작/끝. |
| space.12 | 48px | 헤더-본문 분리 | 큰 타이틀 아래. |
| space.16 | 64px | 랜딩/공백 | 과도 사용 금지(정보 밀도 유지). |

### 1.5 라운드/테두리 토큰 (Radius/Border)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| radius.sm | 8px | 입력/버튼 | 컨트롤 기본. |
| radius.md | 12px | 카드/모달 | 패널류 기본. |
| radius.lg | 16px | 드로어/대형 패널 | 모바일 풀시트에서 사용. |
| radius.full | 9999px | 칩/배지 | pill 형태. |
| border.width.1 | 1px | 기본 보더 | 테이블/입력/카드. |
| border.width.2 | 2px | 포커스/강조 | focus 링 내부 라인. |

### 1.6 그림자 토큰 (Shadow)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| shadow.1 | 0 1px 2px #0F172A14 | 카드 기본 | 배경과 구분만 제공(과장 금지). |
| shadow.2 | 0 6px 16px #0F172A1F | hover/팝오버 | hover 시에만 강화. |
| shadow.3 | 0 16px 40px #0F172A2E | 모달 | 오버레이와 함께 사용. |
| focus.ring | 0 0 0 4px #2563EB38 | 포커스 글로우 | `prefers-reduced-motion`과 무관(접근성). |

### 1.7 모션 토큰 (Motion)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| motion.duration.fast | 120ms | hover 색 변화 | 색/보더 변화 기본. |
| motion.duration.base | 200ms | 모달/드로어 | 오픈/클로즈, 패널 전환. |
| motion.duration.slow | 320ms | 페이지 전환 | 과한 애니메이션 금지. |
| motion.easing.standard | cubic-bezier(0.2,0,0,1) | 대부분 | transform/opacity에 사용. |
| motion.easing.emphasis | cubic-bezier(0.2,0,0,0.6) | 오버레이 | 시작이 빠르고 끝이 부드럽게. |
| motion.reduce | 0ms | 접근성 | `prefers-reduced-motion: reduce` 시 transform 애니메이션 제거. |

### 1.8 Z-index 토큰 (Z)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| z.base | 0 | 기본 | 일반 컨텐츠. |
| z.sticky | 30 | 상단바/필터바 | 스크롤 시 고정. |
| z.dropdown | 40 | 셀렉트/메뉴 | sticky보다 위. |
| z.overlay | 50 | 오버레이 | 모달/드로어 배경. |
| z.modal | 60 | 모달/드로어 | overlay 위. |
| z.toast | 70 | 토스트 | 가장 위(툴팁 제외). |
| z.tooltip | 80 | 툴팁 | 토스트 위, 단 과다 사용 금지. |

---

## 2) 레이아웃 (Layout)

### 2.1 브레이크포인트 (Breakpoints)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| bp.xs | 360px | 소형 모바일 | 최저 기준(가로 스크롤 금지). |
| bp.sm | 480px | 모바일 | 폼/카드 1열. |
| bp.md | 768px | 태블릿 | 2열 카드/8컬럼 그리드. |
| bp.lg | 1024px | 데스크톱 | 사이드바+본문 12컬럼. |
| bp.xl | 1280px | 대형 데스크톱 | 컨테이너 최대폭 적용. |
| bp.2xl | 1440px | 와이드 | 여백 확장, 정보 밀도 유지. |

### 2.2 컨테이너/그리드 (Container/Grid)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| container.maxWidth | 1200px | Web 본문 | 1280px 이상에서도 본문은 1200px로 제한(가독성). |
| container.padding.mobile | 16px | Mobile | 좌우 패딩 고정. |
| container.padding.desktop | 24px | Web | 1024px 이상. |
| grid.columns.mobile | 4 | Mobile | 카드/폼은 4컬럼 기준 배치. |
| grid.columns.tablet | 8 | Tablet | 8컬럼. |
| grid.columns.desktop | 12 | Web | 12컬럼. |
| grid.gutter.mobile | 16px | Mobile | 거터 고정. |
| grid.gutter.desktop | 24px | Web | 거터 고정. |
| section.gap | 24px | 섹션 간격 | 대시보드 패널 간 기본 간격. |
| card.minWidth | 280px | 카드 레이아웃 | 카드가 280px 미만이면 자동 1열로. |

### 2.3 앱 셸 (App Shell)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| topbar.height.web | 64px | Admin/Buyer | 고정(sticky). 왼쪽 로고, 중앙 검색(옵션), 오른쪽 유틸. |
| sidebar.width.expanded | 280px | Admin/Buyer | 데스크톱 기본. |
| sidebar.width.collapsed | 72px | Admin/Buyer | 아이콘만, 툴팁 필수. |
| sidebar.item.height | 44px | Admin/Buyer | 클릭 타깃 44px 이상. |
| bottombar.height.mobile | 56px | Farmer/Logistics | 모바일 하단 탭. 안전 영역(safe-area) 추가 패딩. |
| appbar.height.mobile | 56px | Farmer/Logistics | 상단 앱바. 뒤로/제목/액션. |
| page.title.marginBottom | 16px | Web | 페이지 타이틀 아래 여백. |

### 2.4 스크롤/고정 규칙 (Scroll/Sticky)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| scroll.body | 1 | 전체 | 기본은 body 스크롤 1개만 사용(이중 스크롤 금지). |
| sticky.topbar | z=30 | Web | Topbar는 항상 상단 고정. |
| sticky.filters | top=64px | Web | 테이블/리스트 상단 필터는 Topbar 아래에 sticky. |
| table.scrollRegion.maxHeight | 70vh | Web | 대형 테이블은 내부 스크롤 + 헤더 sticky. |
| modal.body.scroll | auto | 모달 | 모달 컨텐츠가 길면 본문만 스크롤, 헤더/푸터 고정. |
| mobile.safeArea | env(safe-area-inset-*) | Mobile | 하단 탭/버튼이 제스처 영역과 겹치지 않게. |

---

## 3) 컴포넌트 규격 (Components)

### 3.0 공통 상호작용 규칙 (All Interactive)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| hitTarget.min | 44x44px | Mobile 우선 | 아이콘 버튼/체크박스 포함 최소 터치 타깃. |
| focus.ring.inner | 2px #2563EB | 전체 | 키보드 포커스 시 내부 라인. |
| focus.ring.outer | 4px #2563EB38 | 전체 | 외곽 글로우(그림자 토큰). |
| transition.colors | 120ms standard | 전체 | hover/active 색 전환은 120ms. |
| transition.transform | 200ms standard | 카드/드로어 | 레이아웃을 흔드는 scale 애니메이션 금지(필요 시 1.01 이하). |
| cursor.interactive | pointer | Web | 클릭 가능한 카드/행/아이콘 버튼에 반드시 적용. |
| disabled.opacity | 0.55 | 전체 | disabled는 불투명도+색상으로 구분, 클릭 이벤트 차단. |
| loading.pattern | Skeleton/Spinner | 전체 | 400ms 이상 지연이면 skeleton, 짧으면 spinner. |
| error.messaging | 필드 하단 1줄 | 폼 | 에러는 필드 바로 아래, 해결 행동을 포함(예: "숫자만 입력"). |

---

### 3.1 Button

#### 3.1.1 버튼 크기 (Size)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| button.height.sm | 32px | 테이블 내 액션 | 아이콘+텍스트 가능(아이콘 16px). |
| button.height.md | 40px | 기본 | Web 기본. |
| button.height.lg | 48px | 주요 CTA/모바일 | Mobile 기본. |
| button.paddingX.sm | 12px | sm | 텍스트 1~2단어 기준. |
| button.paddingX.md | 16px | md | 기본. |
| button.paddingX.lg | 18px | lg | 모바일 엄지 도달 고려. |
| button.gap | 8px | 아이콘+텍스트 | 아이콘-텍스트 간격. |
| button.radius | 8px | 전체 | `radius.sm` 고정. |
| button.iconOnly.size | 40px | 아이콘 버튼 | 최소 40px, Mobile은 44px 권장. |

#### 3.1.2 버튼 상태/변형 (Variants + States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| button.primary.default | bg #1E7B3A / fg #FFFFFF / border 0 | 주요 CTA | 텍스트는 동사형(예: "샘플 신청"). |
| button.primary.hover | bg #186632 | Web hover | hover는 색만 변화(크기 변화 금지). |
| button.primary.active | bg #145A2C | press | active는 150ms 이내 복귀. |
| button.primary.focus | ring 2px #2563EB + outer 4px glow | 키보드 | 포커스는 색만으로 구분하지 않도록 링 필수. |
| button.primary.disabled | bg #F1F5F9 / fg #94A3B8 | 비활성 | 클릭 불가 + `not-allowed`. |
| button.primary.loading | spinner 16px / text "처리 중" | 비동기 | 로딩 중 중복 클릭 방지(자동 disabled). |
| button.primary.error | bg #DC2626 / fg #FFFFFF | 위험 행동 | 삭제/거절처럼 되돌리기 어려운 작업에만 사용. |
| button.secondary.default | bg #FFFFFF / fg #186632 / border 1px #CBD5E1 | 보조 CTA | 기본은 흰 배경, Primary와 동등한 의미 금지. |
| button.secondary.hover | bg #F2FBF5 / border #1E7B3A | hover | 틴트는 `brand.green.50` 사용. |
| button.secondary.active | bg #DCF3E4 | active | 눌림 느낌은 색/보더로만. |
| button.secondary.focus | ring 동일 | 키보드 | Primary와 동일 규칙. |
| button.secondary.disabled | bg #F1F5F9 / fg #94A3B8 / border #E2E8F0 | 비활성 | 대비가 지나치게 낮아지지 않게. |
| button.secondary.loading | spinner 16px | 비동기 | 텍스트/아이콘은 유지, spinner는 왼쪽. |
| button.secondary.error | fg #B91C1C / border #FCA5A5 | 오류/취소 | "취소"는 secondary로, "삭제"는 danger로. |
| button.tertiary.default | bg transparent / fg #2563EB | 링크형 버튼 | 텍스트 링크와 구분: padding 유지(버튼 형태). |
| button.tertiary.hover | bg #EFF6FF | hover | 배경 틴트만. |
| button.tertiary.active | bg #DBEAFE | active | 진해짐. |
| button.tertiary.focus | ring 동일 | 키보드 | 링 필수. |
| button.tertiary.disabled | fg #94A3B8 | 비활성 | underline 금지. |
| button.tertiary.loading | spinner 16px | 비동기 | 레이아웃 점프 금지(폭 고정). |
| button.tertiary.error | fg #B91C1C | 오류 | 파괴적 행동에는 tertiary 사용 금지. |
| button.danger.default | bg #DC2626 / fg #FFFFFF | 삭제/거절 | 2차 확인(모달)과 함께 사용. |
| button.danger.hover | bg #B91C1C | hover | 더 진하게. |
| button.danger.active | bg #991B1B | active | active는 짧게. |
| button.danger.focus | ring 동일 | 키보드 | 링 유지. |
| button.danger.disabled | bg #F1F5F9 / fg #94A3B8 | 비활성 | 위험 행동 disabled는 이유 툴팁 제공. |
| button.danger.loading | spinner 16px | 비동기 | 로딩 중 취소 버튼 제공. |
| button.danger.error | bg #B91C1C / fg #FFFFFF | 오류 | 동일(에러 상태는 보통 토스트/배너로 처리). |

---

### 3.2 Input (Text/Number/Search/Select 공통)

#### 3.2.1 입력 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| input.height.md | 40px | Web 기본 | 기본 높이. |
| input.height.lg | 48px | Mobile | 터치 타깃 확보. |
| input.paddingX | 12px | 전체 | 좌우 패딩 고정. |
| input.paddingY | 10px | md 기준 | 높이에 맞춰 중앙 정렬. |
| input.radius | 8px | 전체 | `radius.sm`. |
| input.border | 1px #CBD5E1 | default | 기본 보더. |
| input.bg | #FFFFFF | default | hover 시 `#F1F5F9`. |
| input.text | 16px / #0F172A | 본문 | 숫자/단위가 많으므로 최소 16px. |
| input.placeholder | #94A3B8 | 플레이스홀더 | 플레이스홀더는 라벨을 대체하지 않는다. |
| input.label | 14px / #334155 | 라벨 | label 필수(상단). |
| input.helper | 12px / #475569 | 도움말 | 필요 시 1줄만. |
| input.errorText | 12px / #B91C1C | 오류 | 해결 행동 포함 문장. |

#### 3.2.2 입력 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| input.default | bg #FFFFFF / border #CBD5E1 | 기본 | 기본 상태. |
| input.hover | bg #F1F5F9 / border #94A3B8 | Web hover | hover로만 검증 상태를 암시하지 않는다. |
| input.active | border #2563EB | 입력 중 | active는 포커스와 동일하게 처리 가능. |
| input.focus | border #2563EB + ring(2px+glow) | 키보드/클릭 | 포커스 링 필수. |
| input.disabled | bg #F1F5F9 / fg #94A3B8 / border #E2E8F0 | 비활성 | 값 복사 필요 시 readOnly를 별도 제공. |
| input.loading | right spinner 16px | 검색/연동 | 자동완성/공공 API 조회 시 사용. |
| input.error | bg #FEF2F2 / border #FCA5A5 / helper #B91C1C | 검증 실패 | 에러는 즉시 보여주되(blur 또는 제출) 과도한 방해 금지. |

---

### 3.3 Textarea

#### 3.3.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| textarea.minHeight | 96px | 메모/요청사항 | 3줄 기준. |
| textarea.maxHeight | 240px | 긴 입력 | 내부 스크롤 허용. |
| textarea.resize | vertical | Web | Mobile은 resize 비활성 권장. |
| textarea.padding | 12px | 전체 | input과 동일. |
| textarea.radius | 8px | 전체 | input과 동일. |
| textarea.counter | 12px / #475569 | 글자수 | 제한이 있을 때만 노출. |

#### 3.3.2 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| textarea.default | bg #FFFFFF / border #CBD5E1 | 기본 | 기본. |
| textarea.hover | bg #F1F5F9 | Web hover | hover는 배경만. |
| textarea.active | border #2563EB | 입력 중 | active는 포커스와 동일. |
| textarea.focus | border #2563EB + ring | 키보드/클릭 | 링 필수. |
| textarea.disabled | bg #F1F5F9 / fg #94A3B8 | 비활성 | 제출 불가. |
| textarea.loading | placeholder "불러오는 중" | 자동 채움 | AI 도슨트 결과 가져오기 등. |
| textarea.error | bg #FEF2F2 / border #FCA5A5 | 오류 | 에러 메시지 하단. |

---

### 3.4 Card (KPI/리스트/상품/원료 카드)

#### 3.4.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| card.bg | #FFFFFF | 기본 | 캔버스 위 surface. |
| card.border | 1px #E2E8F0 | 기본 | 테두리로 영역 구분. |
| card.radius | 12px | 기본 | `radius.md`. |
| card.padding | 16px | 기본 | 밀도 높은 화면은 12px까지 허용. |
| card.shadow | shadow.1 | 기본 | 기본 그림자. |
| card.header.gap | 8px | 타이틀/액션 | 타이틀과 액션 분리. |
| card.kpi.value | 32px / 600 | KPI | 단위는 caption 12px로 분리. |

#### 3.4.2 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| card.default | border #E2E8F0 / shadow.1 | 기본 | 클릭 불가 카드도 동일. |
| card.hover | shadow.2 / border #CBD5E1 | 클릭 가능한 카드 | hover는 클릭 가능 카드에만 적용. |
| card.active | border #2563EB | 선택/드래그 | 선택 상태는 배경 틴트 대신 보더로. |
| card.focus | ring(2px+glow) | 키보드 | 카드 전체가 링크/버튼이면 focus 적용. |
| card.disabled | opacity 0.55 | 비활성 | 클릭 이벤트 차단. |
| card.loading | skeleton(라인 3개) | 로딩 | 카드 크기 고정(점프 금지). |
| card.error | bg #FEF2F2 / border #FCA5A5 | 오류 | 재시도 버튼 포함. |

---

### 3.5 Modal

#### 3.5.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| modal.scrim | #0F172A66 | 오버레이 | 배경 클릭 시 닫기 여부는 위험도에 따라 결정. |
| modal.width.sm | 360px | 단순 확인 | 모바일에서는 100% - 32px. |
| modal.width.md | 560px | 폼/신청 | 기본. |
| modal.width.lg | 720px | 상세/리포트 | 데스크톱만. |
| modal.radius | 12px | 기본 | `radius.md`. |
| modal.padding | 20px | 기본 | 헤더/바디/푸터 분리. |
| modal.header.height | 56px | 헤더 | 제목+닫기. |
| modal.footer.height | 72px | 푸터 | Primary/Secondary 버튼 우측 정렬. |
| modal.animation | 200ms ease standard | 오픈/클로즈 | opacity+transform(Y 8px). |

#### 3.5.2 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| modal.default | open + focus trap | 기본 | 열릴 때 첫 포커스는 제목 다음의 첫 입력/버튼. |
| modal.hover | 해당 없음 | 컨테이너 | hover/active는 내부 버튼에만 적용. |
| modal.active | 해당 없음 | 컨테이너 | 동일. |
| modal.focus | ring 없음(내부 요소만) | 접근성 | 모달 컨테이너에 링을 주지 말고 내부 포커스 요소로 관리. |
| modal.disabled | primary disabled | 제출 불가 | 폼 미완료 시 primary를 disabled. |
| modal.loading | footer에 spinner + "저장 중" | 비동기 | 닫기/취소 허용 여부를 명시(권장: 취소 허용). |
| modal.error | 상단 error banner + 필드 하이라이트 | 오류 | 서버 오류는 토스트+배너, 필드 오류는 각 필드에. |

---

### 3.6 Drawer (모바일 수거 요청/필터 패널)

#### 3.6.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| drawer.scrim | #0F172A66 | 오버레이 | modal과 동일. |
| drawer.width.desktop | 420px | Web 우측 패널 | 필터/상세. |
| drawer.height.mobile | 80vh | Mobile bottom sheet | 기본 80vh, 드래그로 확장 가능. |
| drawer.radius | 16px | mobile sheet | 상단 모서리만 radius.lg. |
| drawer.padding | 16px | 기본 | 상단 핸들 포함. |
| drawer.animation | 200ms standard | 오픈/클로즈 | slide-in (transform). |
| drawer.handle | 36x4px / #CBD5E1 | Mobile | 핸들은 항상 표시. |

#### 3.6.2 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| drawer.default | open + focus trap(데스크톱) | 기본 | 데스크톱은 키보드 포커스 이동 가능. |
| drawer.hover | 해당 없음 | 컨테이너 | 내부 컨트롤만 hover. |
| drawer.active | drag 중 | Mobile | 드래그 중 배경 스크롤 잠금. |
| drawer.focus | 내부 요소 focus ring | 접근성 | drawer 컨테이너 링 금지. |
| drawer.disabled | CTA disabled | 제출 불가 | 필수값 미입력. |
| drawer.loading | skeleton/inline spinner | 데이터 조회 | 필터 적용/지도 핀 조회. |
| drawer.error | 상단 banner + 재시도 | 오류 | 네트워크 오류 시 캐시/재시도 제공. |

---

### 3.7 Toast (알림)

#### 3.7.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| toast.position.web | top-right 24px | Web | 스택 간격 12px. |
| toast.position.mobile | top 16px | Mobile | 노치 고려 safe-area 적용. |
| toast.width | 360px(max) | Web | 모바일은 100% - 32px. |
| toast.radius | 12px | 전체 | `radius.md`. |
| toast.padding | 12px 14px | 전체 | 아이콘+텍스트. |
| toast.shadow | shadow.2 | 전체 | 떠있는 느낌. |
| toast.duration | 4000ms | 기본 | 성공/정보는 4s, 오류는 6s 권장(수동 닫기 필수). |
| toast.ariaLive | polite/assertive | 접근성 | success/info=polite, error=assertive. |

#### 3.7.2 타입/상태 (Types + States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| toast.success.default | left bar #16A34A / bg #FFFFFF / fg #0F172A | 성공 | 성공은 짧게, 다음 행동 제안. |
| toast.info.default | left bar #2563EB | 정보 | 안내/저장됨. |
| toast.warning.default | left bar #D97706 | 경고 | 주의/확인 필요. |
| toast.error.default | left bar #DC2626 | 오류 | 해결 행동(재시도/문의) 포함. |
| toast.hover | close 버튼 강조 | Web hover | 토스트 전체 hover 확대 금지. |
| toast.active | close 버튼 pressed | 클릭 | close만 active. |
| toast.focus | close 버튼 ring | 키보드 | 닫기 버튼 포커스 가능. |
| toast.disabled | 해당 없음 | 토스트 | 토스트 자체는 disabled 개념 없음(필요 시 버튼 disabled). |
| toast.loading | "처리 중" + spinner | 진행 | 장시간 작업 시작/종료 알림에만. |
| toast.error | error 타입 사용 | 오류 | error는 자동 사라짐 금지(6s)+닫기 제공. |

---

### 3.8 Table (데이터/이력/매칭 리스트)

#### 3.8.1 규격 (Base)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| table.header.height | 44px | Web | 헤더는 sticky 가능. |
| table.row.height | 48px | Web | 밀도 높은 화면은 44px까지 허용. |
| table.cell.paddingX | 12px | 전체 | 숫자열은 오른쪽 정렬. |
| table.cell.paddingY | 10px | 전체 | 세로 정렬 중앙. |
| table.border | 1px #E2E8F0 | 전체 | 외곽+행 구분선. |
| table.header.bg | #F1F5F9 | 헤더 | 헤더 텍스트 14px/600. |
| table.row.hoverBg | #F7FAF8 | hover | 클릭 가능한 행만. |
| table.zebra | off | 기본 | 기본은 zebra off, 필요 시 리포트 화면에서만 on. |
| table.empty.minHeight | 240px | 빈 상태 | 빈 상태는 아이콘+문장+CTA 1개. |

#### 3.8.2 상태 (States)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| table.default | header bg #F1F5F9 / row bg #FFFFFF | 기본 | 기본. |
| table.hover | row bg #F7FAF8 | Web hover | hover는 선택/클릭 가능한 행에만. |
| table.active | row border-left 3px #2563EB | 선택 | 선택은 좌측 인디케이터로 표현(색만으로 구분 금지). |
| table.focus | cell ring 2px #2563EB | 키보드 | 셀 단위 포커스 이동 지원 시. |
| table.disabled | row opacity 0.55 | 권한/잠금 | 행 액션 disabled + 이유 툴팁. |
| table.loading | skeleton rows 6개 | 조회 | 테이블 높이 고정(레이아웃 점프 금지). |
| table.error | inline banner + 재시도 | 오류 | 캐시 데이터가 있으면 캐시 표시 후 재시도 버튼 제공. |

---

## 4) 화면 패턴 (핵심 UX 규칙)

### 4.1 데이터 신뢰성/캐시 표시 (공공 API 지연 대응)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| data.stale.badge | bg #EFF6FF / fg #1D4ED8 | 지연/캐시 | API 실패 시 "캐시 데이터" 배지와 마지막 업데이트 시각을 표기. |
| data.refresh.action | 버튼(tertiary) | 대시보드/지도 | 수동 새로고침 제공. |
| data.timeout.threshold | 4000ms | API 호출 | 4s 초과 시 "지연" 배너 노출 + 캐시 fallback. |
| data.skeleton.delay | 300ms | 로딩 | 300ms 이내면 spinner, 초과면 skeleton. |
| data.lastUpdated.format | YYYY-MM-DD HH:mm | 전체 | 사용자가 비교 가능한 절대시간 사용. |

### 4.2 지도 기반 수급 대시보드 (Map UX)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| map.panel.minHeight | 420px | Web 대시보드 | 지도는 최소 420px 확보(읽기/조작). |
| map.panel.minHeight.mobile | 320px | Mobile | 상단 지도 + 하단 리스트(드로어) 구성. |
| map.pin.size | 24px(default) / 32px(selected) | 지도 핀 | 선택 핀은 크기+외곽 링으로 표현. |
| map.cluster.threshold | 12 pins | 클러스터 | 핀이 12개 이상이면 클러스터링. |
| map.tooltip.delay | 120ms | hover tooltip | hover=120ms, mobile은 tap으로. |
| map.filter.chips | height 32px / radius full | 필터 | 품목/시군/수거가능 시점 칩, 다중 선택 허용. |
| map.list.sync | 1 | 지도+리스트 | 지도 선택=리스트 스크롤/하이라이트 동기화. |

### 4.3 ESG 리포트 (Export/Print)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| report.pageWidth | A4(210mm) | PDF | PDF 내보내기 시 A4 기준. |
| report.kpi.format | 숫자+단위 분리 | 리포트 | tCO2e, kg, ton 등 단위는 캡션. |
| report.chart.altTable | 제공 | 접근성 | 차트는 동일 데이터 테이블을 함께 제공. |
| report.confidence.note | 1줄 | 산출 결과 | "계수/가이드라인 기반 추정" 문구 표기(법적 고지). |

---

## 5) 접근성(A11y) 체크리스트

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 대비(텍스트) | 4.5:1 이상 | 전체 | 본문 16px 기준. 큰 텍스트(18px+ 또는 14px/700)는 3:1 이상. |
| 포커스 표시 | 2px 링 + 4px 글로우 | 전체 | 키보드 탐색 시 항상 보이게. |
| 탭 순서 | 시각적 순서와 동일 | 전체 | 사이드바→본문→푸터 순. |
| 터치 타깃 | 44x44px 이상 | Mobile | 체크박스/아이콘 버튼도 포함. |
| 라벨/설명 | label + helper + error 연계 | 폼 | `aria-describedby`로 도움말/에러 연결. |
| 오류 전달 | 텍스트로 명시 | 폼 | 색만으로 오류 표시 금지(문장/아이콘 병행). |
| 모달 접근성 | `aria-modal`, focus trap | 모달/드로어 | ESC 닫기 제공(필수 입력 폼은 확인 후). |
| 토스트 접근성 | `aria-live` | 토스트 | 에러는 assertive, 자동 사라짐 시간 연장. |
| 표 접근성 | caption/헤더 scope | 테이블 | 열 헤더는 `scope="col"`, 필요 시 행 헤더 제공. |
| 지도 대체 | 리스트/테이블 제공 | 지도 | 핀 데이터는 리스트로도 접근 가능해야 한다. |
| 모션 감소 | reduce 시 0ms | 전체 | transform 애니메이션 제거, 정보 손실 금지. |
| 아이콘 버튼 | aria-label | Web/Mobile | 아이콘만 있는 버튼은 의미 텍스트 제공. |

---

## 6) UI QA 체크리스트 (릴리즈 전)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 반응형 검증 | 360/480/768/1024/1280/1440 | 전체 | 각 구간에서 레이아웃 깨짐/겹침/잘림 없음. |
| 가로 스크롤 | 0 | Mobile | 360px에서 수평 스크롤 발생 금지. |
| 상태 검증 | default/hover/active/focus/disabled/loading/error | 모든 컴포넌트 | 각 상태에서 텍스트/아이콘/보더가 명확히 변한다. |
| 로딩 처리 | skeleton/spinner | 데이터 화면 | 300ms 초과 시 skeleton, 완료 후 점프/깜빡임 없음(CLS). |
| 빈 상태(Empty) | 안내+CTA 1개 | 리스트/테이블 | "데이터 없음"만 표기 금지, 다음 행동 제시. |
| 오류 처리(Error) | 재시도+문의 경로 | 전체 | 서버/네트워크 오류 메시지에 행동 포함. |
| 캐시 폴백 | lastUpdated 표기 | 공공 API | 실패 시 캐시 데이터 표기 및 새로고침 제공. |
| 지도 동기화 | 핀↔리스트 | 지도/대시보드 | 선택 상태가 양쪽에 반영된다. |
| 파일 업로드 | 10MB/타입 제한 | Mobile | 사진 업로드 전 미리보기, 실패 시 원인 표기. |
| 접근성 | 키보드/스크린리더 | Web | 주요 플로우(로그인/신청/저장/삭제) 완주 가능. |
| 권한/보안 UI | 401/403 처리 | 전체 | 세션 만료 시 재로그인 유도, 민감 정보 화면 캡처 주의 문구(필요 시). |

---

## 가정(Assumptions)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---:|---|---|
| 다크 모드 | 미지원 | 전체 | 추후 요구 시 토큰 확장(색/그림자/보더 재정의). |
| 아이콘 세트 | SVG(예: Lucide) | 전체 | 이모지 아이콘 사용 금지. |
| 지도 SDK | 범용(Map SDK 미확정) | 지도 | 핀/클러스터/툴팁 규칙은 SDK와 무관하게 구현 가능하도록 정의. |
| 차트 라이브러리 | 범용(미확정) | 차트 | 시리즈 색/툴팁/대체 테이블 규칙을 우선 적용. |
| 로그인 | OAuth 2.0 기반 | 인증 | UI는 역할별 메뉴 가시성 제어를 포함한다. |

